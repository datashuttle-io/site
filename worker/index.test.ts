import { describe, it, expect } from 'vitest'

// Placeholder — proves the Vitest runner executes against worker/ before the
// Worker skeleton exists. Fleshed out in Task 3 once worker/index.ts lands.
describe('worker/index.ts placeholder', () => {
  it('runner executes', () => {
    expect(true).toBe(true)
  })
})
